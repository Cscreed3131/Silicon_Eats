import 'package:flutter/material.dart';

class AdminManageItemsmWidget extends StatelessWidget {
  const AdminManageItemsmWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hey'),
    );
  }
}
